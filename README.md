# LedgerLogic CLI – 雙式記帳系統

## Proposal Report

### 動機與目標
許多自由工作者、小型商家或個人需要一套**可稽核、符合會計原則**的記帳工具，但市面上的試算表或手機 App 多為單式記帳，無法確保借貸平衡。  
對於開發者而言，一個命令列介面（CLI）的記帳工具能提供快速、鍵盤為中心的操作體驗，不必離開終端機就能管理收支。

本專案的目標是開發一個**命令列雙式記帳系統**，同時作為資料結構效能的實驗平台。使用者能快速記錄交易、使用巨集減少重複輸入，並透過**匯入／匯出**、**自動儲存**確保資料安全。額外實作**復原／重做**、**週期性交易**與**預算追蹤**，使工具具備基礎的個人財務管理能力。  
此外，系統將探索**輸入容錯（Levenshtein Distance）**與**指令別名（alias）**，降低使用門檻。

### 競品比較

| 產品／系統 | 雙式記帳 | CLI 操作 | 內建巨集 | 週期交易 | 預算追蹤 | 復原/重做 | 匯入/匯出 | 自動儲存 | 學習曲線 |
|-----------|---------|---------|---------|---------|---------|----------|----------|----------|----------|
| **本專案** | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ (JSON/CSV) | ✅ | 低 |
| ledger (CLI) | ✅ | ✅ | ❌ (需腳本) | ✅ (手動) | ❌ | ❌ | ✅ (text) | ❌ | 高 |
| hledger | ✅ | ✅ | ❌ | ✅ (指令) | ✅ (基本) | ❌ | ✅ (多格式) | ❌ | 中高 |
| 傳統會計軟體 (ERP) | ✅ | ❌ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | 高 |
| Excel / App | ❌ (單式) | ❌ | ❌ | ❌ | ❌ | ✅ (有限) | ✅ (手動) | ❌ | 低 |

**本專案優勢**：整合多項進階功能於單一輕量 CLI，適合教學、個人記帳與資料結構效能實驗，且程式碼精簡易修改。

### 預期功能
- ✅ 新增交易（借方、貸方、金額、說明、註解）
- ✅ 簡化命令（`simple_expense` 自動從現金扣款）
- ✅ 巨集（午餐、公車、雜支）
- ✅ 查看交易列表（依日期或金額排序）
- ✅ 帳戶餘額報表
- ✅ 刪除交易（自動重新編號）
- ✅ 編輯交易（修改任何欄位）
- ✅ 加入／編輯註解
- ✅ 復原／重做（最多 20 層）
- ✅ 週期性交易（建立規則、到期自動新增）
- ✅ 預算追蹤（每月預算 vs 實際支出）
- ✅ 搜尋交易（關鍵字、金額區間、帳戶）
- ✅ 匯出／匯入 JSON / CSV（合併或取代）
- ✅ 效能測試（比較 list 與 dict 操作速度）
- ✅ 自動儲存（每次變更即寫入檔案）
- ✅ 事件迴圈命令列介面（help, exit）
- 🔲 輸入容錯（Levenshtein Distance 建議正確指令）
- 🔲 指令別名（alias）系統
- 🔲 打包成單一執行檔（PyInstaller）

### 使用技術
- **語言**：Python 3  
- **資料儲存**：JSON 檔案（交易日誌、週期規則、預算設定分開儲存）  
- **命令列介面**：原生 `input()` 事件迴圈 + `argparse` / `cmd` 模組（輔助）  
- **表格顯示**：`rich` 套件（`rich.table`, `rich.panel`, `rich.console`）  
- **匯入／匯出**：Python 內建 `json` 與 `csv` 模組  
- **日期處理**：`datetime`, `timedelta`  
- **效能測試**：自訂 `perftest` 指令，比較 list (O(n)) vs dict (O(1) 平均)  
- **容錯與別名**：`python-Levenshtein`（選配）、自訂 alias 對照表  
- **打包**：`PyInstaller`（選配）  
- **單元測試（選配）**：`unittest` 框架

### Prototype 預計可驗證內容
1. 能否正確儲存與呈現雙式會計交易（借貸平衡）？
2. 巨集是否有效減少重複輸入？
3. 復原／重做是否正確還原前後狀態？
4. 週期性交易能否自動在到期日新增並更新下次到期日？
5. 預算追蹤是否準確計算當月支出並與預算比較？
6. 匯出後的 JSON/CSV 能否完整匯入（含註解）而不遺失資料？
7. 使用 dict 儲存時，刪除與查詢操作是否明顯比 list 版本快？
8. 事件迴圈是否能流暢處理多詞參數指令（如 `recur add`）？
9. （若實作）輸入容錯是否能正確建議相似指令？
10. （若實作）別名系統是否能正確展開為原始命令？

---

## Prototype Report

### 目前進度
已完成**事件迴圈雙式記帳 CLI**原型，已通過單元測試的核心功能如下：

- ✅ **交易管理**：`add`, `list`, `balance`, `delete`, `edit`, `comment`  
- ✅ **簡化命令**：`misc <amount>` 直接記為 Miscellaneous ↔ Cash  
- ✅ **巨集系統**：內建 `lunch` (Food↔Cash)、`bus` (Transport↔EasyCard)  
- ✅ **復原／重做**：每項修改操作前自動推入堆疊，支援 20 層歷史  
- ✅ **週期性交易**：`recur add/list/delete/run`  
- ✅ **預算追蹤**：`budget set/show`，比較當月實際支出與預算  
- ✅ **搜尋**：關鍵字搜尋或互動式多條件篩選  
- ✅ **匯入／匯出**：支援 JSON 與 CSV，可合併或取代  
- ✅ **效能測試**：`perftest` 指令載入 10,000 筆測試資料，實際比較 list 與 dict 在隨機查詢及刪除後重建的執行時間  
- ✅ **自動儲存**：所有寫入操作立即存檔  
- ✅ **美觀報表**：使用 `rich` 顯示彩色表格，餘額正負以綠／紅標示  
- ✅ **資料完整性**：刪除交易後自動重新編號 ID，匯入時處理型別轉換

待辦事項：

**程式碼結構**：單一檔案約 600 行，包含資料管理、核心功能、巨集、週期、預算、匯出入、效能測試與主迴圈。

### 遇到的困難
1. **CSV 匯入的型別問題**  
   - 問題：CSV 讀取時所有欄位皆為字串，導致金額與 ID 無法計算及排序。  
   - 解決：在 `import_transactions` 中明確將 `id` 轉為 `int`、`amount` 轉為 `float`，並確保 `comment` 欄位存在。

2. **重新編號與復原堆疊的互動**  
   - 問題：刪除交易後重新編號，若使用者復原，舊 ID 會與目前狀態衝突。  
   - 解決：復原／重做時直接覆蓋整個 transactions dict，不依賴 ID 連續性；ID 重整僅在刪除後立即執行，復原時會還原完整的 dict（含原來的 ID）。

3. **週期交易的「下次到期日」更新邏輯**  
   - 問題：若使用者手動修改交易日期，可能造成週期規則與實際記錄不一致。  
   - 解決：週期規則獨立儲存於 `recurring.json`，`recur run` 僅根據規則的 `next_due` 新增交易，不依賴已存在的交易記錄。

4. **預算計算的帳戶範圍**  
   - 問題：所有借方帳戶都會被計入支出，但收入帳戶（如薪資）不應納入預算消耗。  
   - 解決：`budget show` 僅對已經設定預算的帳戶進行統計，未設定預算的借方帳戶忽略。

5. **大量測試資料產生與效能比較的再現性**  
   - 問題：測試資料需隨機但可重複，否則 `perftest` 結果難以比較。  
   - 解決：提供 `generate_test_data.py` 可產生固定隨機種子 (`random.seed(42)`) 的 10,000 筆交易。

### 下一步計畫
- 短期（Prototype 之後，Final 之前）：
  - 實作**輸入容錯**：使用 Levenshtein Distance 建議正確指令（例如 `edt` → `edit`）。
  - 實作**別名系統**：讓使用者自訂 `alias`（如 `ls` = `list date`）。
  - 使用 **PyInstaller** 打包成單一執行檔，方便非 Python 環境使用。
- 長期（課程結束後擴充）：
  - 圖形化報表（`matplotlib` 支出圓餅圖）
  - 多使用者／多帳本
  - 資料庫後端（SQLite）

---

## Final Report
<!-- 留待期末填寫：完整專案說明、使用方式、課程關聯總結（須納入效能分析比較的說明） -->
